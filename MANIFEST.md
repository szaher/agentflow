# Agentflow v0.1 package manifest

- `src/agentflow/` — meta-harness implementation
- `src/agentflow/harnesses/` — Claude Code, Codex, Pi, OpenCode and custom-command adapters
- `src/agentflow/builtin_patterns/` — 21 executable Agentic SDLC patterns
- `tests/` — pattern, bootstrap, harness, Git fingerprint, gate, risk and engine tests
- `examples/` — custom harness and pattern examples
- `.github/workflows/ci.yml` — Python 3.11–3.13 CI
- `install.sh` / `uninstall.sh` — local installation helpers
- `README.md` — user guide
- `ARCHITECTURE.md` — architectural boundaries
